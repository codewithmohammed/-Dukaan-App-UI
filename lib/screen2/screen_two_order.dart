import 'package:flutter/material.dart';

class ScreenTwoOrder extends StatelessWidget {
  const ScreenTwoOrder({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
