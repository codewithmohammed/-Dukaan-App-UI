import 'package:flutter/material.dart';

class ScreenTwoProducts extends StatelessWidget {
  const ScreenTwoProducts({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
