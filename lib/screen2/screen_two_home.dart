import 'package:flutter/material.dart';

class ScreenTwoHome extends StatelessWidget {
  const ScreenTwoHome({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
